{% extends "ParabolAdminCoreBundle::base_admin_uncompressed.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block body_content %}

<style>

.account-wall .form-signin
{
    padding: 15px 30px;
}
.account-wall .form-signin .form-control
{
    position: relative;
}
.account-wall .form-signin .form-control:focus
{
    z-index: 2;
}

.loginContainer {
    padding-top: 120px;
}

.account-wall
{
    padding: 40px 0px 20px 0px;
    background-color: #ffffff;
    box-shadow: 0 2px 10px 0 rgba(0, 0, 0, 0.16);
}

.account-wall .profile-img
{
    width: 96px;
    height: 96px;
    margin: 0 auto 10px;
    display: block;
    -moz-border-radius: 50%;
    -webkit-border-radius: 50%;
    border-radius: 50%;
}

.input-group {
    width: 100%;
    position: relative;
}

.input-group span {
    position: absolute;
    left: 14px;
    top: 16px;
    z-index: 1000;
    font-size: 100%;
    color: #999;
}

.input-group input {
    padding-left: 40px;
}

#username:focus {
    z-index: 999;
}

body {
    background-color: #222D32;
}

label {
    font-weight: normal;
}

</style>

<div class="container">
    <div class="row">
        <div class="col-sm-6 col-md-4 col-md-offset-4 loginContainer">

            {% if error %}
                <div class="alert alert-danger alert-dismissable">
                    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
                    <i class="icon fa fa-ban"></i> {{ error.messageKey|trans(error.messageData, 'security') }} 
                    
                  </div>
            {% endif %}
            <div class="account-wall span1 box box-primary">
                
               		    	<img class="profile-img" src="/bundles/admingeneratorgenerator/img/unauthenticated.png" alt="">
	               			<form class="form-signin" action="{{ path("fos_user_security_check") }}" method="post">
	               				<div class="form-group">
		               				<input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                                    <div class="input-group">
                                    <span class="glyphicon glyphicon-user"></span>                                        
      	               				<input style="margin-bottom: -1px;" type="text" id="username" name="_username" value="{{ last_username }}" class="form-control input-lg" placeholder="Username" required autofocus>
                                    </div>
                                    <div class="input-group">
                                        <span class="glyphicon glyphicon-lock"></span>
		               				<input type="password" id="password" name="_password" class="form-control input-lg" placeholder="{{ 'security.login.password'|trans }}" required>
                                    </div>
		               			</div>
		               			<div class="form-group">	
	               				<input  type="submit" id="_submit" name="_submit" class="btn btn-lg btn-default btn-block" value="{{ 'security.login.submit'|trans }}" />
	               				</div>
	               				<div class="form-group">
	               					<label><input type="checkbox" id="remember_me" name="_remember_me" value="on" /> {{ 'security.login.remember_me'|trans }}</label>
	               				</div>
	               			</form>
               				<!--div id="tabs" data-tabs="tabs">
	               				<p class="text-center"><a href="#register" data-toggle="tab">Need an Account?</a></p>
              				</div-->
			    </div>
	        </div>
	    </div>
	</div>
</div>

{% endblock body_content %}
